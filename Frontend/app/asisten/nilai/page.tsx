"use client";
import AppShell from "@/components/AppShell";
import { Save } from "@/components/icons";
import { assistantAssessments, classes, grades } from "@/lib/data";
import { useState } from "react";
export default function AsistenNilai(){const [assessment,setAssessment]=useState("Keaktifan");const [classId,setClassId]=useState(classes[0].id);return <AppShell><div className="page"><div className="page-heading"><div><h1 className="page-title">Input Nilai Asisten</h1><p className="page-subtitle">Kelola nilai tugas, keaktifan, praktikum, remidi, dan final praktikum.</p></div></div><div className="card"><div className="filters"><div className="field"><label>Kelas</label><select className="select" value={classId} onChange={e=>setClassId(e.target.value)}>{classes.map(c=><option key={c.id}>{c.name}</option>)}</select></div><div className="field"><label>Komponen Nilai</label><select className="select" value={assessment} onChange={e=>setAssessment(e.target.value)}>{assistantAssessments.map(a=><option key={a}>{a}</option>)}</select></div><div className="field"><label>Semester</label><select className="select"><option>Gasal 2026/2027</option></select></div></div><div className="table-wrap"><table><thead><tr><th>Student ID</th><th>Nama Mahasiswa</th><th>{assessment}</th><th>Catatan</th></tr></thead><tbody>{grades.map(g=><tr key={g.id}><td>{g.id}</td><td>{g.name}</td><td><input className="score-input" type="number" min="0" max="100" defaultValue={Math.min(100, g.tugas + (assessment==="Keaktifan"?8:assessment.includes("Praktikum")?4:0))}/></td><td><input className="input" placeholder="Opsional"/></td></tr>)}</tbody></table></div><div style={{display:"flex",justifyContent:"flex-end",marginTop:16}}><button className="btn primary"><Save size={15}/> Simpan Nilai</button></div></div></div></AppShell>}
